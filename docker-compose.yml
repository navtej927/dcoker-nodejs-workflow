version: '3'

services:
  node-app:
    build: .
    # environment:
    #   - PORT=9003
    volumes:
      - ./:/app:ro
      - /app/node_modules
    ports:
      - 9000:9000
    command: npm run start